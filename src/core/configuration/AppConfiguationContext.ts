import type {ReactNode} from 'react';

export const appConfigContext:{
  defaultLogo: string,
  defaultTitle: string,
  defaultSignUpDescription?: ReactNode,
  defaultSignUpDisclaimer?: ReactNode,
} = {
  defaultLogo: ``,
  defaultTitle: ``,
}